// VoxBabel — REST API Client
const API_BASE = '/api';

export async function createMeeting() {
  // TODO: POST /api/meetings
}

export async function joinMeeting(roomId, userName, language) {
  // TODO: POST /api/meetings/:roomId/join
}
